// $(document).on('ready', () => {
	// var iframe = document.querySelector('iframe');
	// const player = new playerjs.Player(iframe);
	

	// player.on('ready', () => player.play());
	// player.play();

	// var playerResponds = false;

	// player.on('ready', () => {
	// 	player.on('play', () => {
	// 		console.log('play');
	// 	});

	// 	if (player.supports('method', 'mute')) {
	// 		player.mute();
	// 	}

	// 	// playerResponds = true;
	// 	player.play();

	// });

