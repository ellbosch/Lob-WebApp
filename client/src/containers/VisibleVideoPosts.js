// import { connect } from react-redux;
// import VideoPostList from '../components/VideoPostList';
// import { VisibilityFilters } from '../actions';

// const getVideoPosts = (videoPosts, filter, channel=null) => {
//     switch (filter) {
//         case VisibilityFilters.SELECT_ALL:
//             return videoPosts
//         case VisibilityFilters.SELECT_CHANNEL:
//             return 
//         default:
//             throw new Error('Unknown filter: ' + filter)
//     }
// }

// const mapStateToProps = state => ({
//     videoPosts: getVideoPosts(state.videoPosts, state.visibilityFilter)
// });

// export default connect(mapStateToProps)(VideoPostList);